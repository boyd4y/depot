


<%= stylesheet_link_tag 'gmaps4rails' %>
<%= gmaps({
        "map_options" => { 
        		"language" => "zh-CN",
          		"auto_adjust" => true,
          		"zoom" => 5
        	},
        "markers"     => { "data" => data, "options" => {
        		"do_clustering" => true
        		}
        	}
        })
%>
<%= yield :scripts %>